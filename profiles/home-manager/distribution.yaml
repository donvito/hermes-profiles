# Hermes profile distribution manifest.
# Schema: hermes_cli/profile_distribution.py (DistributionManifest) in hermes-agent.
name: home-manager
version: 0.1.0
description: "Household assistant — meal planning, maintenance, and reminders"
hermes_requires: ">=0.18.0"
author: "hermes-profiles maintainers"
license: "MIT"

env_requires:
  - name: OPENROUTER_API_KEY
    description: "Model provider key (or run `hermes -p home-manager setup` to pick another provider)"
    required: false
